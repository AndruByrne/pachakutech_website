ok back to the header redesign--probably all of today's work, hopefully just that.
We are going to implement this design:
when the header collapses, where the circle stops will be the "home" button position (not labeled on the home page, just the location where the circles are) and the buttons next to it on the right will read 'edu' 'eval' and 'elev' in my custom font, reflecting the section id;
the buttons will have a transparent background--letting the header color show through--and the font color will fade in from transparent to the primary color in the same time as the wheels turn from secondary to primary (lasthalfturnlerp).
These buttons will only be active when they are becoming visible (the "last half turn lerp"), if the header is collapsed, then when the user clicks on that button the header will not change size; however, since one can get from the home page to the top section when the header is not fully collapsed, it would be best to use our existing flight animation for all these cases;
the flight animation will be used to complete a triangl of sorts, changing the background color if it is collapsing(as it now does) but moving the destination location of the wheels; they should roatate and translate a fraction of the final lerp based on the target button position i.e. the wheels rotate to the full 584 degrees when going to home, but then--there being three sections--should rotate to 539 degrees for the leftmost button (by the order of AppSection enum that would be 'eval') then rotate to 494 for the second section(edu) then to 449 for the last, which happens to be elev at this time.
we learned in a previous session that the destination page context will be providing the parameters for hero animations; as they exist after the home page, they know more information.
on the the detail pages, this nav will appear with the buttons remainging in their place, but with the circles spinning "rolling" to those degrees mentioned and translating to cover up + replace whatever section + button is being selected

To the right of that left-hand nav section there should be padding ofc and then the ticker; the ticker does not need to be centered horizontally; it should be taking up the rest of the header by the end of the animation but have the width of the "dotLogoDiameter" when beginning it's fade-in.
We will simply cross-fade from the dot_logo to the Marquee (already imported) (with velocity 0 and Loaded with the same text as is in the logo) during the lasthalfturnlerp. this will require us to shift our current dot_logo scaling forwards: the scaling should begin just as the header begins to collapse and end at the time it currently starts -- angle 404  degrees IIRC--right when lasthalfturnlerp starts. This way, when we fade it out over that "last half turn" lerp and replace it with the Marquee widget fading in, we do not have to consider "scaling text" *shudder*.

as the Marquee fades in during that lasthalfturnlerp, we move the velocity of the Marquee from 1-20.

The Marquee Does NOT need to have the detail section-correct ticker while animating; it needs to actually be just "pachakutech" to manage that cross-fade with the dot-logo image; only once the detail page is loaded do we really need to be even scrolling the marquee

For page loading reasons, I encourage you to consider the structure we had to use in the home_content for the ContentSectionCards: the Futures (ultimately representing dependencies and their asyncronies) makes for double page-laoding; I'm hesitant to be passing futures through the flight animaton! but encourage you to keep the same interface here:
